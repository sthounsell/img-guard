{
  "name": "img-guard-external-comparison",
  "version": "0.1.0",
  "private": true,
  "description": "Benchmark harness comparing img-guard's own phash/lookup implementation against Radu's four Node candidates (issue #12) — isolated from node/package.json so comparison-only dependencies never enter img-guard's real dependency tree.",
  "type": "commonjs",
  "scripts": {
    "benchmark:compute": "node scripts/run-compute.js",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "vitest": "^4.1.11"
  }
}
